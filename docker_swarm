docker service create --name rolling-nginx --replicas 3 nginx:1.25

docker service update \
  --image nginx:latest \
  --update-parallelism 1 \
  --update-delay 10s \
  rolling-nginx

docker service ps rolling-nginx

docker service update --rollback rolling-nginx

#########################################################

docker service logs <service_name>
docker service create --name <name> <image>
docker service ls
docker service ps <service_name>
docker service inspect <service_name>
docker service logs <service_name>
docker service scale <service_name>=<number>
docker service update --image <new_image> <service_name>
docker service update --publish-add <port>:<port> <service_name>
docker service rollback <service_name>
docker service rm <service_name>
